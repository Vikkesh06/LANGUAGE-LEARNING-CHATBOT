services:
  - type: web
    name: language-learning-chatbot
    env: python
    buildCommand: apt-get update && apt-get install -y build-essential gfortran libatlas-base-dev && python -m pip install --upgrade pip setuptools && pip install --only-binary numpy,pandas numpy==1.24.1 pandas==1.5.3 && pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false 